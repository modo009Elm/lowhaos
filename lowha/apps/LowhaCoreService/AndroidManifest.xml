<?xml version="1.0" encoding="utf-8"?>
<!--
     Copyright (C) 2026 The LowhaOS Project
     SPDX-License-Identifier: Apache-2.0
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="app.lowha.core"
    android:sharedUserId="android.uid.system">

    <!--
         PACKAGE_USAGE_STATS  - read foreground time (the daily budget)
         SUSPEND_APPS         - the enforcement mechanism itself
         RECEIVE_BOOT_COMPLETED - reconcile suspension after a reboot
         WRITE_SETTINGS       - persist limit and app lists in Settings.System

         Running as android.uid.system exempts this app from the privapp
         permission allowlist, which is why no allowlist entry is needed here.
    -->
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS" />
    <uses-permission android:name="android.permission.SUSPEND_APPS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WRITE_SETTINGS" />
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />

    <application
        android:label="@string/app_name"
        android:icon="@drawable/lowha_logo"
        android:persistent="true"
        android:directBootAware="true">

        <service
            android:name=".LowhaCoreService"
            android:exported="true"
            android:directBootAware="true">
            <intent-filter>
                <action android:name="app.lowha.core.ILowhaCoreService" />
                <action android:name="app.lowha.core.action.COMMIT" />
            </intent-filter>
        </service>

        <receiver
            android:name=".BootReceiver"
            android:exported="true"
            android:directBootAware="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
                <action android:name="app.lowha.core.action.MIDNIGHT_RESET" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
